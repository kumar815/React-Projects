import React from "react";

class Footer extends React.Component {
        constructor(props) {
            super(this.props);

            render() {
                return ( <
                    React.Fragment >
                    <
                    h2 > React Footer Component < /h2>{" "} < /
                    React.Fragment >
                );
            }
        }
        export default Footer;